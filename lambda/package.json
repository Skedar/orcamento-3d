{
  "name": "orcamento-3d-lambda",
  "version": "1.0.0",
  "description": "Processador de arquivos STL para orçamento 3D",
  "main": "index.js",
  "dependencies": {
    "stl-parser": "^0.8.1"
  },
  "scripts": {
    "build": "powershell Compress-Archive -Path * -DestinationPath function.zip -Force",
    "deploy": "aws lambda update-function-code --function-name orcamento-3d-upload --zip-file fileb://function.zip"
  }
} 